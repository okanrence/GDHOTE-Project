@model List<GDHOTE.Hub.CoreObject.ViewModels.PublicationAccessRightViewModel>

@{
    ViewBag.Title = "Access Rights";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Publication Access Rights</h2>
<p>
    @Html.ActionLink("New Access Right", "New", "PublicationAccessRight", null, new { @class = "btn btn-primary" })
</p>


@if (Model == null || Model.Count == 0)
{
    <p>No record(s) found</p>
}
else
{
    <div id="responsive">
        <div class="form-group">
            @Html.AntiForgeryToken()
        </div>

        <div>
            <table id="accessrights" class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <th>Publication</th>
                        <th>Status</th>
                        <th>Date</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var accessRight in Model)
                    {
                        <tr>
                            <td>@Html.ActionLink(accessRight.Name, "Edit", "PublicationAccessRight", new { id = accessRight.Id }, null)</td>
                            <td>@accessRight.Status</td>
                            <td>@accessRight.DateCreated.ToString("dd-MMM-yyyy")</td>
                            <td><button data-access-right-id="@accessRight.Id" class="btn btn-link js-delete">Delete</button></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

    </div>

}
@section scripts
{
    <script>
        $(document).ready(function () {
            $("#accessrights").DataTable();

            $("#accessrights").on("click", ".js-delete", function () {
                var button = $(this);
                var id = button.attr("data-access-right-id");

                var form = $('#responsive');
                var token = $('input[name="__RequestVerificationToken"]', form).val();

                //Submit request
                var request = { __RequestVerificationToken: token };

                bootbox.confirm("Are you sure you want to proceed?", function (result) {
                    if (result) {
                        $.ajax({
                            url: '@Url.Action("DeletePublicationAccessRight", "PublicationAccessRight")/' + id,
                            method: "POST",
                            data: request,
                            success: function (data) {
                                if (data !== null) {
                                    if (data.ErrorCode === "00") {
                                        swal({
                                            title: "Success",
                                            text: "success",
                                            type: "success"
                                        });

                                        window.location.reload(true);
                                    }
                                    else {
                                        alert(data.ErrorMessage);
                                    }
                                };

                            }
                        });
                    }
                });

            });
        });
    </script>
}

